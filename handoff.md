# Handoff Deprecated

`handoff.md` 已废弃，不再作为本项目的新会话交接文件。

请读取并维护：

1. `AGENTS.md`
2. `relay-task.md`
3. `project-board.md`

原因：`/wrap-up` 技能已经使用 `relay-task.md` 作为接力单。为避免两个交接文件
并行维护导致状态冲突，本项目从 2026-05-15 起只使用 `relay-task.md`。
