# 知识库结构

_2026-05-17 · 个人知识体系整体架构,跨"App 侧"与"知识库管理侧"两端_

## 一、核心理念

借鉴 Karpathy 的 LLM Wiki 方法论:**不在查询时翻原文,而是让 LLM 平时就维护一个互链的 markdown wiki**。知识的瓶颈不是读和想,是没人愿意持续记账——而这正是 LLM 不偷懒的事。

之前用 graphify 复刻效果差的根因:**产物形态错**(产"图"不可读)+ **缺概念页中间层**(链接稀疏)。本体系纠正为:产物坚持给人读的 Markdown 综述,核心是 concepts 概念页。

## 二、职责一刀切:App 侧 vs 知识库管理侧

```
┌─ PersonalAgent App(采集+导出) ──┐      ┌─ 知识库管理 Claude(结构化+复利) ─┐
│ 草稿全文 + results.jsonl 翻译历史 │      │ /kb add <App 导出的原料包>         │
│ → 一键导出原料包(用户选位置)    │ 文件 │ → 结构化 / 改旧页 / 去重 / 建链接   │
│                                  │ ───→ │ → ~/knowledge/ 复利                │
│ 不调 LLM、不结构化、不并入        │ 解耦 │ LLM 处理只发生在这一侧             │
└──────────────────────────────────┘      └────────────────────────────────────┘
        详见 design-note-export-pack.md          已落地(/kb skill + ~/knowledge)
```

**两端用文件解耦,互不知道对方实现。** App 只产出干净的结构化原料单篇;合并、去重、复利全交下游 Claude。这让 App 侧极简(无需读已有库、无合并逻辑),知识库侧专注它最擅长的记账。

## 三、知识库管理侧:~/knowledge 结构

### 三层架构

| 层 | 目录 | 可变性 | 作用 |
|---|---|---|---|
| Raw | `raw/` | 只读不改 | 原始素材(文章+URL / 视频要点+链接 / App 导出的原料包) |
| Concepts | `concepts/` | **反复改写** | ★核心层。每页一主题,聚合所有相关素材要点 + James 判断 + 引用 |
| Notes | `notes/` | 增量 | 还没成主题的零散想法、待沉淀洞见 |

可读性和多边链接**都长在 concepts 层**。raw 是证据,notes 是草料,concepts 是成品。

### 关键文件

- `SCHEMA.md` — 元规则(怎么 ingest/链接/体检)。James 调教知识库 = 改这里
- `index.md` — 按主题分类的人读入口
- `log.md` — append-only 日志(每次 ingest/query/lint 一行)

### 三个操作(/kb skill)

- **Ingest** `/kb add` — 吸收素材。★核心纪律:**默认改已有 concept 页,不新建**(链接密集的前提是概念页被复用)
- **Query** `/kb ask` — 只查 concepts/notes 不翻原文;有价值的综合回写进 concept(复利)
- **Lint** `/kb lint` — 扫矛盾/过期/孤立页/缺链接/结构违规,出清单不自动改

### Concept 页标准结构(直接针对"可读性差")

一句话(30秒看懂)→ 核心(连贯叙述非 bullet 堆砌)→ 关联(至少 1 条 `[[链接]]` 带"为什么相关")→ 来源证据。James 的个人判断**必须显式标 `判断:`**,与客观信息分开。

## 四、与其他沉淀机制的边界(职责不混)

| 机制 | 管什么 | 寿命 |
|---|---|---|
| **本知识库** `~/knowledge` | 长期要读和复用的领域知识 | 很长,跨项目 |
| auto memory `.../memory/` | Claude 跨会话上下文 | 中,项目级 |
| relay-task.md | 任务接力 | 短,项目内交接 |
| ~/baidu/Archives/ | 对话原文存档 | 长,只存不读 |
| graphify(可选) | 偶尔"发现没想到的关联" | 看完即弃 |

三者用 `[[链接]]` 互指但职责不混。同一来源(如做完某功能)会同时进不同机制,但存的内容不同:auto memory 存"怎么决策的给下轮接力",知识库存"提炼的领域知识给未来复用"。

## 五、当前状态

- ✅ `~/knowledge/` 骨架 + SCHEMA + 2 概念种子页(karpathy-llm-wiki / macos-swift-pitfalls)+ xfyun-tts
- ✅ `/kb` skill 三命令(add/ask/lint)已落地并被系统识别
- ⏳ App 侧「导出原料包」+「多草稿 Tab」功能 — 架构设计已出(design-note-export-pack.md,含多草稿附章),待 James 拍板再写代码
