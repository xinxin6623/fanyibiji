 ⚙️ 2026-05-30 上线 Syncthing 跨机同步：以下两路径已通过 syncthing 实时同步到火山云 ECS
    （folder ID `pa-appsupport` / `pa-words`），三端 P2P，秒级回传。详细运维见
    `~/Documents/my_huoshan_web/ADMIN.md §6`。`tts-audio/`、`_audio/` 已 .stignore 排除。

 1. App 临时/常规存储（macOS Application Support）

  路径：~/Library/Application Support/com.james.personalagent/

  hotkey-config.json      快捷键配置
  language-config.json    语言配置
  llm-config.json         baseUrl/model
  prompt-config.json      系统提示词
  tts-config.json         发音人/语速等
  secrets.enc             加密密钥库（AES-GCM，已替代 Keychain）
  results.jsonl           翻译/查询历史（每行一条）
  notes/                  笔记草稿目录（draft-<id>.md + sidecar + manifest.json）
  tts-audio/              划词朗读 MP3 缓存

  定义在 PersonalAgent/App/AppComposition.swift:28-193，applicationSupportDirectory 拿不到才回落
  temporaryDirectory（极少触发）。

  2. 词典双击保存的词卡

  路径：~/knowledge/words/（不在 Application Support，跟笔记知识库并存，可被 [[wiki-link]] 命中）

  ~/knowledge/words/
  └── <word>.md              单词卡：frontmatter + 渲染正文（文件名小写归一）
                             frontmatter 的 us_audio / uk_audio / audio 留远程 MP3 链接
                             正文 [🔊](https://...) 直接指向远端 URL

  2026-05-30 改：不再下载 MP3 到 _audio/，只在 md 里保留链接。如果盘上还残留旧的
  _audio/ 目录，可手动删除（不影响新词卡）。

  定义在 PersonalAgent/App/AppComposition.swift 的 makeWordCardStore 和
  PersonalAgent/Core/Persistence/WordCardStore.swift。当前盘上有：compose.md / context.md / graceful.md / query.md /
  tags.md / worker.md。